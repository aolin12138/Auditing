interesting, so let me tell u my flow in the presentation. im doing the second part, where we peer would finish the opeiong and all the way to the pipeline slides(the we report the spread slide would be before the pipeline slide in the final one). so i will be mainly be evaluating on the results. here are the ideas of the order of things im ganna be talking about.

1. first i will be mentioning like, "in the experiment, there are three hyperparam that we needed to tune to see their effect on the result which is the relationship between the spread and the defetc. the model we train on the poisoned dataset, the adversarial attack we used on the test points, in this case would be one white box whichb is the tree atttack, and one would be black box which is the hotskipjump attack, and also the defect  type since different types of defect can bed reflected differently on the spread metric"
2. and then this would be on the coverage gap slide, we say like "this plot shows all the possible combinations of attacks and models we used under the bias defect which is trhe coverage gap, and we see a general position trend in all of them while the test accuracy stayed flat. so the bias is successfully reflected onb the spread, however, it  is noted that, when bias jumped from 0.7 to 0.9, u cuold notice that the spread actually dropped when the attack is hsj. while it is still steady and increasing for the white box attack." im not sure the explanmnation for this one? i might say "this is likely due to the fact that with black box attack, since it doesnt have the model's internal information, the attack is done by approximation and hence is undeterminstic unlike the white box attack, and when the bias increased to 9, u almost deleted all points hence the decision boundary actually becomes quite simple, and causing the adversarial points to land close to each other again" how is the attack actually done with hsj? like do we pick like the test points in the dataset, and then hsj initialise a ranomy data point which is a noise bascilly, and try to move that as close to the test point as possible? so say if we have n test points, we would get n adversaril attack points right? this is interest9ing so the intialisation matters as well? since the point can land on local minimal as well, but why simple decision boundary would casue tthis?
3. then we move onto the label noise slide(we dont have a slide with the label noise against the spread plot yet in the ppt yet make one. there is a plot we had for the report, but thats before 0.5, most of teh plots we got for label noise is under 0,.5 right now, since above 0.5 would be random, however i still want a full plot, so we can show them and i would talk about the randomness as well). so i will say som,ething like "now looking at the label noise(in the plot in the report, why do i only see tree + dta where is tree + hsj? i know the trend is flat but we still need to include and also talk abnout why its flat? any idea?) we could still observe a trend where the adversarial points tend to scatter when the lable noise klevel increases to 0.5 where 50% of the label flips. hoiwever, after 0.5 the sprea d changes randomly. and even before 0.5 we can see that the test accuracy actually drops as the label noise increase meaning that this defect is actully not hidden, adn we dont teally need the metric to reveal. but we still wanted to dive a bit deeper on why the metric fails after 0.5, and our hypothesis was that, when u have too many random labels in a disrtubution, it would intyroduce a large amount of decision boundries, which could trap the attack points(so for hsj the intialisation wont be able to move anywhere since its surrounded by deciosion boundries, even mov ing slighly it will flip class so it cant really approach the target class), hence since the initialisation is random, the distrubution we measured would be random as well. looking at the compression ratio(wait i just realised if we using compression ratio, that should mean we start at the test point location and then tries to move out of the decisoon boudnary? rather than what i thoyght in hsj we tries to move close to the test popint without crossing the boundry? which is it exactly?) and the pertubatiuon also decaraese, meaning that the attack had to move less distance to flip the class(yes this also assunmes we starting at the test point, need to verify which one it is actully)
4. then i would condlude stuff like, "we have verfied for cov erage gap, our mteric successfully revealed the hidden defect, while it could fail on labvel noise, even tho its not hidden so we dont need the metric. however, this could mean on different combination, like model change or attack chgange, or on different defect, the metric is very likely to response differently, and we suspec t that a single metric can do trhe job all the time. in the fureture, we would test more defetcts that would be realisatic and happens in real dattset, like outliers,a nd also tries more models like random forest opr xgboost to see wherther they possess trhe same attribute. and most importantly, we might need to come up with a set of metrics, not just relying on a single one, with a single one it can fail in some cases, but with a set of metrics, some might fail in some cases, howver its hard foir them to all fail at the same time" d ddo
